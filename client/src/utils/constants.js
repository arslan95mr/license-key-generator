export default class Keys {
    static EMAIL_REGEX = /^[a-zA-Z][a-zA-Z0-9-_.]{3,31}$/;

    static LICENSE_KEYS_LINKS = [ '/', '/generate'];
    
    static SUBSCRIPTION_TYPE = ['MONTHLY', 'YEARLY', 'LIFETIME'];
}